// backtesting_screen.cpp — split into focused files.
// All implementations live in:
//   bt_screen.cpp   — render(), render_top_nav, render_command_panel
//   bt_config.cpp   — all render_*_config methods (backtest/advanced/optimize/walk-forward/indicator/etc.)
//   bt_results.cpp  — render_results_panel and all render_result_* methods
//   bt_exec.cpp     — render_status_bar, pickup_result, execute_command, init_strategy_params
